type FormSettings2Props = {
  keyNumber: number;
  onUpdate: (value: number, value2: number) => void;
};
export default function FormSettings2({
  keyNumber,
  onUpdate,
}: FormSettings2Props) {
  return <p>FormSettings2</p>;
}

// import { useEffect, useState } from "react";
// import { useForm } from "react-hook-form";
// import type { SubmitHandler, SubmitErrorHandler } from "react-hook-form";
// import Form from "react-bootstrap/Form";
// import Button from "react-bootstrap/Button";
// import { getData, getWeekArray } from "../../utils/common";
// import type { Inputs } from "../../types/inputs.type";

type FormSettings3Props = {
  keyNumber: number;
  onUpdate: (value: number, value2: number) => void;
};
export default function FormSettings3({
  keyNumber,
  onUpdate,
}: FormSettings3Props) {
  // const weeklyGoalPeriod = getWeekArray(2025, 8, 1);
  // console.log(weeklyGoalPeriod);

  return <p>FormSettings3</p>;
}

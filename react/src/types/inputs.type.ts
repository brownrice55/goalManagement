export type Inputs = {
  goal: string;
  annualGoals: string[];
  monthlyGoals: string[];
  monthlyGoalsPeriod: (number | number[])[][];
  weeklyGoals: string[];
  weeklyGoalsPeriod: (number | number[])[][];
  status: number;
  date: string;
  hasPeriod: string;
  period: string;
  customDate: string;
  diff: number;
  startDateArray: number[][];
  endDateArray: number[][];
  endDate: number[];
};
